# Copy to .env / .env.local and adjust if your Azure Functions host uses another port.
# Visual Studio / local.settings "Host.LocalHttpPort" is usually 7005.
VITE_API_BASE_URL=http://localhost:7005/api
