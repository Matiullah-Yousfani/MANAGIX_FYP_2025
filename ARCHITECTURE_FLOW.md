# 🏗️ Architecture & Data Flow

## 🔄 Complete System Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                          USER JOURNEY                                │
└─────────────────────────────────────────────────────────────────────┘

1. User logs in → userId stored in localStorage
2. User navigates to /resume-upload
3. User selects PDF/DOCX file
4. User clicks "Parse Resume"
5. System parses and displays data
6. User reviews and edits information
7. User clicks "Save Profile"
8. System saves to database
9. User redirected to dashboard

┌─────────────────────────────────────────────────────────────────────┐
│                        TECHNICAL FLOW                                │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│  1. React UI     │  User uploads resume.pdf
│  ResumeUpload    │  
│  Component       │
└────────┬─────────┘
         │
         │ (1) File → Base64 conversion
         │     resumeService.fileToBase64(file)
         ↓
┌────────────────────────────────────────┐
│  2. API Call                           │
│  resumeService.parseResume({           │
│    userId: "guid",                     │
│    fileName: "resume.pdf",             │
│    fileBase64: "JVBERi0xLjQK..."       │
│  })                                    │
└────────┬───────────────────────────────┘
         │
         │ HTTP POST
         │ http://localhost:7071/api/resume/parse
         ↓
┌──────────────────────────────────────────┐
│  3. .NET Azure Function                  │
│  ResumeFunction.ParseResume()            │
│                                          │
│  - Receives upload request               │
│  - Validates data                        │
│  - Prepares request for Python service   │
└────────┬─────────────────────────────────┘
         │
         │ HTTP POST
         │ http://localhost:8000/parse-resume
         │ {
         │   "filename": "resume.pdf",
         │   "file_base64": "JVBERi0..."
         │ }
         ↓
┌────────────────────────────────────────────┐
│  4. Python FastAPI Service                 │
│  fastapi_app.py                            │
│                                            │
│  ┌─────────────────────────────────────┐  │
│  │ Decode base64 → file bytes          │  │
│  └─────────────────────────────────────┘  │
│           ↓                                │
│  ┌─────────────────────────────────────┐  │
│  │ Extract text (PDF/DOCX)             │  │
│  │ - pdfplumber for PDF                │  │
│  │ - python-docx for DOCX              │  │
│  └─────────────────────────────────────┘  │
│           ↓                                │
│  ┌─────────────────────────────────────┐  │
│  │ Call Groq LLM API                   │  │
│  │ - Send resume text                  │  │
│  │ - Use llama-3.1-8b-instant model    │  │
│  │ - Parse to structured JSON          │  │
│  └─────────────────────────────────────┘  │
│           ↓                                │
│  ┌─────────────────────────────────────┐  │
│  │ Generate Summary (if needed)        │  │
│  │ - Another Groq call                 │  │
│  │ - Professional summary generation   │  │
│  └─────────────────────────────────────┘  │
│           ↓                                │
│  ┌─────────────────────────────────────┐  │
│  │ Return structured JSON:             │  │
│  │ {                                   │  │
│  │   "name": "John Doe",               │  │
│  │   "email": "john@example.com",      │  │
│  │   "phone": "+123456789",            │  │
│  │   "summary": "Experienced...",      │  │
│  │   "education": [...],               │  │
│  │   "skills": [...],                  │  │
│  │   "projects": [...],                │  │
│  │   "experience": [...]               │  │
│  │ }                                   │  │
│  └─────────────────────────────────────┘  │
└────────┬───────────────────────────────────┘
         │
         │ JSON Response
         ↓
┌──────────────────────────────────────┐
│  5. .NET Backend Receives Response   │
│  ResumeFunction.ParseResume()        │
│                                      │
│  - Deserializes JSON                 │
│  - Returns to React frontend         │
└────────┬─────────────────────────────┘
         │
         │ HTTP 200 OK + JSON Data
         ↓
┌────────────────────────────────────────┐
│  6. React UI Displays Data             │
│  ResumeUpload Component                │
│                                        │
│  - Populates form fields               │
│  - User can edit all information       │
│  - Add/remove skills                   │
│  - Add/remove education                │
│  - Add/remove projects                 │
│  - Add/remove experience               │
└────────┬───────────────────────────────┘
         │
         │ User clicks "Save Profile"
         │
         │ (7) API Call
         │ resumeService.saveResumeProfile({
         │   userId, name, email, phone,
         │   summary, education, skills,
         │   projects, experience
         │ })
         ↓
┌────────────────────────────────────────────┐
│  8. .NET Azure Function                    │
│  ResumeFunction.SaveResumeProfile()        │
│                                            │
│  Calls → ResumeService                     │
└────────┬───────────────────────────────────┘
         │
         ↓
┌────────────────────────────────────────────┐
│  9. ResumeService.SaveResumeProfileAsync() │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │ Update User (FullName)               │ │
│  │ → Users table                        │ │
│  └──────────────────────────────────────┘ │
│           ↓                                │
│  ┌──────────────────────────────────────┐ │
│  │ Update/Create UserProfile            │ │
│  │ → UserProfiles table                 │ │
│  │   (Phone, Summary)                   │ │
│  └──────────────────────────────────────┘ │
│           ↓                                │
│  ┌──────────────────────────────────────┐ │
│  │ Clear existing resume data           │ │
│  │ → Remove old education               │ │
│  │ → Remove old skills                  │ │
│  │ → Remove old projects                │ │
│  │ → Remove old experience              │ │
│  └──────────────────────────────────────┘ │
│           ↓                                │
│  ┌──────────────────────────────────────┐ │
│  │ Insert new Education entries         │ │
│  │ → ResumeEducations table             │ │
│  └──────────────────────────────────────┘ │
│           ↓                                │
│  ┌──────────────────────────────────────┐ │
│  │ Insert new Skills                    │ │
│  │ → ResumeSkills table                 │ │
│  └──────────────────────────────────────┘ │
│           ↓                                │
│  ┌──────────────────────────────────────┐ │
│  │ Insert new Projects                  │ │
│  │ → ResumeProjects table               │ │
│  └──────────────────────────────────────┘ │
│           ↓                                │
│  ┌──────────────────────────────────────┐ │
│  │ Insert new Experience                │ │
│  │ → ResumeExperiences table            │ │
│  └──────────────────────────────────────┘ │
│           ↓                                │
│  ┌──────────────────────────────────────┐ │
│  │ Commit transaction                   │ │
│  │ → SaveChangesAsync()                 │ │
│  └──────────────────────────────────────┘ │
└────────┬───────────────────────────────────┘
         │
         │ Success Response
         ↓
┌──────────────────────────────────────┐
│  10. React UI                        │
│  - Shows success message             │
│  - Redirects to /dashboard           │
└──────────────────────────────────────┘
```

---

## 📊 Database Relationships

```
┌──────────────────┐
│     Users        │
│ ─────────────────│
│ UserId (PK)      │◄────────┐
│ FullName         │         │
│ Email            │         │
│ PasswordHash     │         │
│ CreatedAt        │         │
└──────────────────┘         │
         △                   │
         │ 1:1               │
         │                   │
┌──────────────────┐         │
│  UserProfiles    │         │
│ ─────────────────│         │
│ ProfileId (PK)   │         │
│ UserId (FK) ─────┤         │
│ Phone            │         │
│ Address          │         │
│ Summary ✨ NEW   │         │
│ ...              │         │
└──────────────────┘         │
                             │
         ┌───────────────────┼───────────────────┬───────────────────┐
         │ 1:N               │ 1:N               │ 1:N              │ 1:N
         │                   │                   │                  │
┌────────────────┐  ┌──────────────────┐  ┌─────────────────┐  ┌──────────────────┐
│ResumeEducations│  │  ResumeSkills    │  │ ResumeProjects  │  │ResumeExperiences │
│────────────────│  │──────────────────│  │─────────────────│  │──────────────────│
│EducationId(PK) │  │SkillId (PK)      │  │ProjectId (PK)   │  │ExperienceId (PK) │
│UserId (FK) ────┤  │UserId (FK) ──────┤  │UserId (FK) ─────┤  │UserId (FK) ──────┤
│Degree          │  │SkillName         │  │Title            │  │Title             │
│Institution     │  │CreatedAt         │  │Description      │  │Company           │
│Year            │  └──────────────────┘  │CreatedAt        │  │Duration          │
│Details         │                        └─────────────────┘  │Description       │
│CreatedAt       │                                             │CreatedAt         │
└────────────────┘                                             └──────────────────┘
```

---

## 🔧 Component Architecture

### Backend Components

```
┌─────────────────────────────────────────────────────────┐
│                   Azure Function                        │
│  ┌───────────────────────────────────────────────────┐ │
│  │            ResumeFunction.cs                      │ │
│  │  - ParseResume() → Parse file via Python         │ │
│  │  - SaveResumeProfile() → Save to DB              │ │
│  │  - GetResumeProfile() → Retrieve data            │ │
│  └───────────────┬───────────────────────────────────┘ │
└──────────────────┼─────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────┐
│                   Service Layer                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │              ResumeService.cs                     │ │
│  │  - SaveResumeProfileAsync()                      │ │
│  │  - GetResumeDataAsync()                          │ │
│  └───────────────┬───────────────────────────────────┘ │
└──────────────────┼─────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────┐
│                Repository Layer (UnitOfWork)            │
│  ┌───────────────────────────────────────────────────┐ │
│  │  - ResumeEducationRepository                     │ │
│  │  - ResumeSkillRepository                         │ │
│  │  - ResumeProjectRepository                       │ │
│  │  - ResumeExperienceRepository                    │ │
│  │  - UserProfileRepository                         │ │
│  │  - UserRepository                                │ │
│  └───────────────┬───────────────────────────────────┘ │
└──────────────────┼─────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────┐
│               Entity Framework Core                     │
│              ApplicationDbContext                       │
│  ┌───────────────────────────────────────────────────┐ │
│  │  DbSet<ResumeEducation>                          │ │
│  │  DbSet<ResumeSkill>                              │ │
│  │  DbSet<ResumeProject>                            │ │
│  │  DbSet<ResumeExperience>                         │ │
│  │  DbSet<UserProfile>                              │ │
│  └───────────────┬───────────────────────────────────┘ │
└──────────────────┼─────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────┐
│                  SQL Server Database                    │
│  - ResumeEducations                                     │
│  - ResumeSkills                                         │
│  - ResumeProjects                                       │
│  - ResumeExperiences                                    │
│  - UserProfiles (with Summary)                          │
└─────────────────────────────────────────────────────────┘
```

### Frontend Components

```
┌─────────────────────────────────────────────────────────┐
│                   React Application                     │
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │              ResumeUpload.tsx                     │ │
│  │                                                   │ │
│  │  State Management:                               │ │
│  │  - selectedFile                                  │ │
│  │  - isUploading                                   │ │
│  │  - isParsing                                     │ │
│  │  - parsedData                                    │ │
│  │  - formData (editable)                           │ │
│  │  - error                                         │ │
│  │                                                   │ │
│  │  Handlers:                                       │ │
│  │  - handleFileChange()                            │ │
│  │  - handleUploadAndParse()                        │ │
│  │  - handleSaveProfile()                           │ │
│  │  - updateFormField()                             │ │
│  │  - addSkill/Education/Project/Experience()       │ │
│  │  - removeSkill/Education/Project/Experience()    │ │
│  └───────────────┬───────────────────────────────────┘ │
└──────────────────┼─────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────┐
│                API Service Layer                        │
│  ┌───────────────────────────────────────────────────┐ │
│  │            resumeService.ts                       │ │
│  │  - parseResume()                                 │ │
│  │  - saveResumeProfile()                           │ │
│  │  - getResumeProfile()                            │ │
│  │  - fileToBase64()                                │ │
│  └───────────────┬───────────────────────────────────┘ │
└──────────────────┼─────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────┐
│                  Axios Instance                         │
│  - Base URL configuration                               │
│  - HTTP methods                                         │
│  - Request/Response interceptors                        │
└─────────────────────────────────────────────────────────┘
```

---

## 🎨 UI Component Hierarchy

```
ResumeUpload Component
│
├── Upload Section (when no parsed data)
│   ├── File Input with Custom Label
│   ├── Upload Icon & Text
│   └── Parse Button (with loading state)
│
├── Error Message (conditional)
│   ├── Error Icon
│   └── Error Text
│
└── Parsed Data Section (when data available)
    │
    ├── Section Header
    │   ├── Title with emoji
    │   └── Subtitle
    │
    ├── Form
    │   │
    │   ├── Basic Information Section
    │   │   ├── Name Input
    │   │   ├── Email Input
    │   │   ├── Phone Input
    │   │   └── Summary Textarea
    │   │
    │   ├── Skills Section
    │   │   ├── Section Header with Add Button
    │   │   └── Skills Grid
    │   │       └── Skill Items (input + remove button)
    │   │
    │   ├── Education Section
    │   │   ├── Section Header with Add Button
    │   │   └── Education Cards
    │   │       ├── Degree Input
    │   │       ├── Institution Input
    │   │       ├── Year Input
    │   │       ├── Details Input
    │   │       └── Remove Button
    │   │
    │   ├── Projects Section
    │   │   ├── Section Header with Add Button
    │   │   └── Project Cards
    │   │       ├── Title Input
    │   │       ├── Description Textarea
    │   │       └── Remove Button
    │   │
    │   ├── Experience Section
    │   │   ├── Section Header with Add Button
    │   │   └── Experience Cards
    │   │       ├── Title Input
    │   │       ├── Company Input
    │   │       ├── Duration Input
    │   │       ├── Description Textarea
    │   │       └── Remove Button
    │   │
    │   └── Form Actions
    │       ├── Upload Another Button
    │       └── Save Profile Button (with loading state)
```

---

## 🔄 State Flow

```
Initial State
    ↓
User Selects File
    ↓
File Validated → selectedFile updated
    ↓
User Clicks "Parse Resume"
    ↓
isUploading = true, isParsing = true
    ↓
File converted to Base64
    ↓
API call to backend
    ↓
Backend calls Python service
    ↓
Python parses with Groq
    ↓
JSON returned to backend
    ↓
Backend returns to frontend
    ↓
parsedData = response, formData = response
isUploading = false, isParsing = false
    ↓
Form displayed with parsed data
    ↓
User edits fields (formData updated)
    ↓
User clicks "Save Profile"
    ↓
isUploading = true
    ↓
API call to save data
    ↓
Backend saves to database
    ↓
Success → Navigate to dashboard
```

---

## 🌐 Network Flow

```
Frontend (localhost:5173)
    ↕ HTTP
Backend (localhost:7071)
    ↕ HTTP
Python Service (localhost:8000)
    ↕ HTTPS
Groq API (api.groq.com)

Backend (localhost:7071)
    ↕ SQL Protocol
SQL Server Database
```

---

This architecture ensures:
- ✅ Clean separation of concerns
- ✅ Scalability at each layer
- ✅ Easy testing and maintenance
- ✅ Production-ready structure
- ✅ Following your existing patterns
